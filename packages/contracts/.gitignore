# Forge build output and broadcast logs. `out/` is regenerated by `forge build`;
# `ts/abi.ts` is generated from it and IS committed, because the other packages
# import it and a fresh clone must typecheck without a Solidity toolchain.
out/
cache/
broadcast/
