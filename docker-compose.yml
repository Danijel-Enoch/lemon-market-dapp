services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: prerelease
    restart: always
    ports:
      - "3002:3002"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    environment:
      - NODE_ENV=development
    command: bun run dev
